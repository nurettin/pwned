#include <map>
#include <string>
#include <vector>
#include "io.hpp"

int main()
{
/* TODO: output to stream and check for eq
	using pwned::p;
	double a[]= { 0.5, 0.3, 0.2 };
	std::vector<double> v(a, a+ 3);
	std::map<std::string, int> m;
	m["a"]= 42;
	m["b"]= 44;
	p(a);
	p(v);
	p(m);
*/
}

